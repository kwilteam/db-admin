database greeting_8;

table messages {
  id text maxlen(21) primary notnull,
  message text notnull maxlen(100),
  wallet text notnull,
  #wallet_index index(wallet)
}

action insert_message ($id, $message) owner {
  INSERT INTO messages
  VALUES ($id, $message, address(@caller));
}