import React from "react"

export default function Login() {
  // If admin account does not exist then redirect to setup page

  // Once logged in, redirect to dashboard page
  return <div>Login</div>
}
